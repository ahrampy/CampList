import React, { Component } from 'react';
import { withRouter } from 'react-router-dom';

export default class SiteDetail extends Component {

  render() {
    const {
      author, name, date, description, stieFeatures
    } = this.props.site

    return (
      <div>
        
      </div>
    )
  }
}
